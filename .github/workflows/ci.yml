name: FixList CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    name: Lint, typecheck, contract tests, and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply guarded product patch
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/fix-release-authority-classifier-v5'
        shell: bash
        run: |
          set -o pipefail
          python .github/scripts/apply_release_classifier_patch.py 2>&1 | tee frontend-patch-application.log

      - name: Upload frontend patch failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-patch-application
          path: frontend-patch-application.log

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Frontend contract tests
        run: npm run test:frontend

      - name: Capture frontend failure summary
        if: failure()
        run: npm run test:frontend > frontend-failure.txt 2>&1 || true

      - name: Upload frontend failure summary
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-failure-summary
          path: frontend-failure.txt

      - name: Build
        run: npm run build

  scanner-regressions:
    name: Scanner regression fixtures
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip pytest pytest-asyncio
          pip install -r scanner-api/requirements.txt

      - name: Apply guarded product patch
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/fix-release-authority-classifier-v5'
        shell: bash
        run: |
          set -o pipefail
          python .github/scripts/apply_release_classifier_patch.py 2>&1 | tee scanner-patch-application.log

      - name: Upload scanner patch failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scanner-patch-application
          path: scanner-patch-application.log

      - name: Run root scanner regression tests
        run: pytest tests/

      - name: Run Python scanner-api tests
        working-directory: scanner-api
        run: pytest tests/

      - name: Capture scanner failure summary
        if: failure()
        working-directory: scanner-api
        run: pytest tests/ -q --tb=short > ../scanner-failure.txt 2>&1 || true

      - name: Upload scanner failure summary
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scanner-failure-summary
          path: scanner-failure.txt

      - name: Verify frozen beta revision matches code
        working-directory: scanner-api
        run: python scripts/freeze_beta_revision.py --check

  publish-tested-patch:
    name: Publish tested product patch
    if: github.event_name == 'pull_request' && github.head_ref == 'agent/fix-release-authority-classifier-v5'
    needs: [build, scanner-regressions]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout patch branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply patch to branch tree
        run: python .github/scripts/apply_release_classifier_patch.py

      - name: Remove patch harness and restore standard CI
        run: |
          git fetch origin main
          git checkout origin/main -- .github/workflows/ci.yml
          rm .github/scripts/apply_release_classifier_patch.py
          rm .github/scripts/patch_payload_*.txt

      - name: Commit tested product changes
        run: |
          git config user.name "seo-pilot-ci"
          git config user.email "seo-pilot-ci@users.noreply.github.com"
          git add -A
          git commit -m "Prioritize business-relevant FixList evidence"
          git push origin HEAD:${{ github.head_ref }}
