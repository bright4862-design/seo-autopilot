name: FixList CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    name: Lint, typecheck, contract tests, and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply focused classifier patch
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/classifier-v6-saas-weighting'
        run: python tools/apply_saas_weighting_patch.py

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Frontend contract tests
        run: npm run test:frontend

      - name: Build
        run: npm run build

  scanner-regressions:
    name: Scanner regression fixtures
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip pytest pytest-asyncio
          pip install -r scanner-api/requirements.txt

      - name: Apply focused classifier patch
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/classifier-v6-saas-weighting'
        run: python tools/apply_saas_weighting_patch.py

      - name: Run focused classifier matrix
        working-directory: scanner-api
        run: pytest tests/test_classifier_v6_saas_weighting.py -q

      - name: Run root scanner regression tests
        run: pytest tests/

      - name: Run Python scanner-api tests
        working-directory: scanner-api
        run: pytest tests/

      - name: Verify frozen beta revision matches code
        working-directory: scanner-api
        run: python scripts/freeze_beta_revision.py --check

  publish-tested-patch:
    name: Publish tested classifier patch
    if: github.event_name == 'pull_request' && github.head_ref == 'agent/classifier-v6-saas-weighting'
    needs: [build, scanner-regressions]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout patch branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply tested product changes
        run: python tools/apply_saas_weighting_patch.py

      - name: Remove one-shot harness and restore normal CI
        run: |
          git fetch origin main
          git checkout origin/main -- .github/workflows/ci.yml
          rm tools/apply_saas_weighting_patch.py

      - name: Commit tested product changes
        run: |
          git config user.name "seo-pilot-ci"
          git config user.email "seo-pilot-ci@users.noreply.github.com"
          git add -A
          git commit -m "Fix SaaS classifier weighting for content-heavy sites"
          git push origin HEAD:${{ github.head_ref }}
