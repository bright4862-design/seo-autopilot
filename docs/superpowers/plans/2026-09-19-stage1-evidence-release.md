# Stage one evidence correctness release

Authority: the approved full scanner blueprint and the user's subsequent instruction, “once stage one is ready deploy and publish.” Stage one is the first release milestone; B06–B24 coverage/ranking work and the genuine 30-site full-blueprint gate remain open. They must not be claimed complete by this release.

## Scope and invariants

Complete B01–B05, the stage-one synthetic portion of B25, B27 historical/GEO compatibility, and B28 exact-source deployment/authorized live acceptance. Keep Standard 150, safe request/robots/authority rules, historical signatures, free-preview privacy and all V6 public routes. No new requests or external provider integration in this stage.

## Execution

1. Finish published identity Task 3 completion gate and Task 4: real Python producer → sealed persisted rows → verified customer projection → cards/JSON/PDF/CSV/copy. Retain legacy serialization; preserve new observed route spellings with trusted identity context. Exercise actual serializers and PDF annotation output. Root owns this coupled migration.
2. Add B03/B04 detector evidence and tests. A bounded accepted-HTML adapter distinguishes absent/empty alt and material/uncertain/excluded image applicability, and detects visible unresolved templates with placement and bounded snippets. Hidden/code content is excluded; legitimate coming-soon announcements are not shells. Use one confirmed repair per template cause. Version changed detector semantics and retain legacy reconstructed reports. A dedicated writer may own extract.py, the new helper, review_calibration.py, and detector tests; root integrates review.py/scanner.py consumers serially.
3. Add B05 search-facing relevance. Intentional utility noindex and validated canonical-away pages have no independent search metadata repairs; sitemap or explicit search-intent conflicts have one index-or-drop decision. Keep accessibility findings, canonical problems and access evidence. Prove repeated canonical annotation is stable and mixed-family counts only include applicable pages.
4. Build the named synthetic acceptance corpus and scripts/assertCorpusRun.mjs. Execute actual source with labelled fixtures for Pretto, Center Street, both Ike's hosts, getfixlist and Ironwood negatives. No historical/live counts masquerade as observations. Wire the stage-one corpus into CI; the full 30-site replay gate stays explicitly unverified until genuine paired inputs exist.
5. Run full root/scanner/frontend, lint/type/build, package/contract, historical/tamper/privacy/security checks. Fresh-context whole-branch review, correct important findings, and generate the canonical candidate release artifacts. Merge only exact reviewed source with fresh CI.
6. Revalidate production access and exact current source. Use the existing controlled admission cutover; named schema comparison, six V6 runtime checks, exact site publishing, matching staged worker promotion and rollback readiness, authorized Funbooker scan plus exact result reload. Do not claim deployment from a staging or CLI success alone.

## Parallel ownership

Current root: frontend/export identity and integration. Read-only auditors may examine all modules. Detector writer excludes scanner.py/review.py, package mirrors, generated files and shared ledgers. Consumer integration and all release mutations remain serialized with root. Record RED/GREEN commands and inspected failure causes for every behavior change.
